package it.polimi.ingsw.am13.client.gamestate;

import org.junit.jupiter.api.Test;

public class TestGameState {

    @Test
    public void testCreation() {
//        assertEquals(3, state.getPlayers().size());
//        assertTrue(state.getPlayers().contains(List.of(ps[0], ps[1], ps[2])));
//
////            assertEquals(ps[2], ((MsgResponseStartGame)liss[i].updates.getLast()).getGameState());
        // TODO: Fai test per costruttore usando model dato da update di lobby
    }

}